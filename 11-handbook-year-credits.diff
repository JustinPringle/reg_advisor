diff --git a/programmes/augmented_civil.yaml b/programmes/augmented_civil.yaml
index e3d868e..d7677ee 100644
--- a/programmes/augmented_civil.yaml
+++ b/programmes/augmented_civil.yaml
@@ -46,6 +46,14 @@ external_prereqs:
 # say so HERE with a reason rather than restating the field on the module. The
 # loader applies these, and catalogue_lint reports an undeclared disagreement as
 # an error rather than a shrug. Empty block = the programme agrees with ITS.
+# Degree credits per year of study, AS PRINTED IN THE HANDBOOK. This is the
+# arithmetic the module list below must satisfy; catalogue_lint --credits checks
+# it and the loader warns when the list drifts. Y1 and Y2 are printed module by
+# module in the handbook and agree with the list below. Y3-Y5 are printed as
+# year totals only, so this block is the only record of them.
+#   64 + 112 + 128 + 128 + 144 = 576
+year_credits: {1: 64, 2: 112, 3: 128, 4: 128, 5: 144}
+
 catalogue_overrides:
   # TWO KINDS OF CREDIT. Degree credit is what counts toward the 576 needed to
   # graduate, and every ERS progression threshold is cumulative degree credit.
diff --git a/programmes/civil.yaml b/programmes/civil.yaml
index 80acf2c..0c06b3b 100644
--- a/programmes/civil.yaml
+++ b/programmes/civil.yaml
@@ -33,6 +33,10 @@ programme:
   name: Civil Engineering
   total_credits: 576
 regulation_years: 4
+# Degree credits per year of study, from the handbook. Checked by
+# catalogue_lint --credits; the loader warns if the module list drifts from it.
+#   144 x 4 = 576
+year_credits: {1: 144, 2: 144, 3: 144, 4: 144}
 # Augmented <-> mainstream first-year twins (handbook equivalence table). An
 # augmented-entry student who articulates into mainstream Civil sits these eight
 # modules under their augmented codes; each is the handbook equivalent of a
